/*try{
  _satellite.setVar('thisClickURL',this.href);
  var urlVal = this.href;
  if(urlVal.indexOf('exitLinkName') > -1){
  	var exitLinkName = s.Util.getQueryParam("exitLinkName", urlVal,"?");
  	var exitLinkName = s.Util.getQueryParam("exitLinkName", urlVal,"&");
 	 _satellite.setVar('COVID_exitLinkName',exitLinkName);
    _satellite.track('covidExitLink');
  }
  else if(urlVal.indexOf('intLinkName') > -1){
    var intLinkName = s.Util.getQueryParam("intLinkName", urlVal);
		_satellite.setVar('COVID_intLinkName',intLinkName);
    _satellite.track('covidIntLink');
  }
}catch(e){};
*/
